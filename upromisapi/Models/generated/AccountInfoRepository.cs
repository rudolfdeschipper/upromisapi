/*
**             ------ IMPORTANT ------
** This file was generated by ZeroCode2 on 03/Jan/2021 22:50:28
** DO NOT MODIFY IT, as it can be regenerated at any moment.
** If you need this file changed, change the underlying model or its template.
*/
using APIUtils.APIMessaging;
using Microsoft.EntityFrameworkCore;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Linq.Dynamic.Core;

namespace upromiscontractapi.Models
{

    public class AccountInfoRepository : IAccountInfoRepository
    {
        ContractDbContext Context { get; set; }

        public AccountInfoRepository(ContractDbContext context)
        {
            Context = context;

        }
        public IQueryable<AccountInfo> List => Context.Accounts
        ;

        public async Task<(List<AccountInfo>, double)> FilteredAndSortedList(SortAndFilterInformation sortAndFilterInfo, bool paging)
        {
            return await DoSortFilterAndPaging(sortAndFilterInfo, paging);
        }

        private async Task<(List<AccountInfo>, double)> DoSortFilterAndPaging(SortAndFilterInformation sentModel, bool doPaging)
        {
            string whereClause = String.Empty;
            var records = List;
            int filteredCount = records.Count();

            if (sentModel != null)
            {
                string[] strings = {
                        "code",
                        "title",
                        "description",

                        ""
                };
                // filtering
                // column search is handled here:0
                if (sentModel.filtered != null)
                {
                    foreach (var item in sentModel.filtered)
                    {
                        if (!String.IsNullOrEmpty(item.value))
                        {
                                if(strings.Contains(item.id))
                                {
                                    whereClause = whereClause + item.id + ".Contains(\"" + item.value + "\") &&";
                                }
                                else
                                {
                                    whereClause = whereClause + item.id + ".ToString().Contains(\"" + item.value + "\") &&";
                                }
                        }
                    }
                    if (!string.IsNullOrEmpty(whereClause))
                    {
                        whereClause = whereClause[0..^2];
                        records = records.Where(whereClause);
                        filteredCount = await records.CountAsync();
                    }
                }
                // ordering
                if (sentModel.sorted != null)
                {
                    string orderBy = "";
                    foreach (var o in sentModel.sorted)
                    {
                        orderBy += " " + o.id + (o.desc ? " DESC" : " ASC") + ",";
                    }
                    if (orderBy.EndsWith(','))
                    {
                        orderBy = orderBy[0..^1];
                    }
                    if (!string.IsNullOrEmpty(orderBy))
                    {
                        records = records.OrderBy(orderBy);
                    }
                }

                // paging:
                if (doPaging)
                {
                    records = records
                        .Skip(sentModel.page * sentModel.pageSize)
                        .Take(sentModel.pageSize);
                }
            }
            return (await records.ToListAsync(), sentModel?.pageSize != 0 ? Math.Ceiling((double)(filteredCount / sentModel.pageSize)) : 1.0);
        }


        public async Task<AccountInfo> Post(AccountInfo rec)
        {
            var record = rec;
            // TODO: add validations
            // TODO: check for new users to create in the team composition list
            record.AccountFields.ForEach(pi =>
            {
                pi.AccountInfo = record;
            });
            record.Teammembers.ForEach(pi =>
            {
                pi.AccountInfo = record;
            });

//            record.AccountInfo = Context.AccountInfo.FirstOrDefault();

//            record.ParentContract = Context.Contracts.Find(rec.DataSubject.ParentContractID);

            Context.Accounts.Add(record);

            await Context.SaveChangesAsync();

            return record;
        }

        public async Task<AccountInfo> Put(AccountInfo rec)
        {
            Context.Accounts.Attach(rec);

            await Context.SaveChangesAsync();

            return rec;
        }

        public async Task<bool> Delete(AccountInfo rec)
        {
            var ctr = Context.Accounts.FirstOrDefault(c => c.ID == rec.ID);

            if (ctr == null)
            {
                return false;
            }

            Context.Accounts.Remove(ctr);

            await Context.SaveChangesAsync();

            return true;
        }

        public async Task<AccountInfo> Get(int id)
        {
            var record = await Context.Accounts.Where(c => c.ID == id)
                .Include( p => p.AccountFields)
                .Include( p => p.Teammembers)
                .Include( p => p.Contracts)
                .Include( p => p.Proposals)
                .Include( p => p.Requests)
                .FirstOrDefaultAsync();
            return record;
        }


        public async Task<List<ListValue>> GetProposalIdReference()
        {
        var ct = Context;
        var records = ct.Proposals;
        return await records.Select( s => new ListValue() { Value = s.ID, Label = s.Title}).ToListAsync();
}

    }
}
